<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <h1>UsernameL <%= user.username %></h1>
    <% if(user.pets){ %>
        <div>
            <h3><%= pet.name %></h3>
            <p><%= pet.type %></p>
            <p><%= pet.color %></p>
            <p><%= pet.birthday %></p>
        </div>

   <% } %>
</body>
</html>